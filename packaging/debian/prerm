#!/bin/sh
set -e

if [ "$1" = "remove" ] || [ "$1" = "deconfigure" ]; then
	if command -v systemctl >/dev/null 2>&1; then
		systemctl stop broadcast-box-nat-refresh.timer >/dev/null 2>&1 || true
		systemctl stop broadcast-box.service >/dev/null 2>&1 || true
	fi
fi

exit 0
